name: Build OpenClaw Skill

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'skills/**'
      - 'references/**'
      - 'scripts/**'
      - 'toolkit/**'
      - 'personas/**'
      - 'requirements.txt'
      - '*.example.yaml'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Context budget guard (#16)
        run: |
          python3 scripts/context_budget.py
          # v2.0 模块化：峰值 = 主入口 + 5 个管道模块 SKILL.md + on-path references，
          # 比 v1.x 单体多 ~2.5k tokens（各模块自带运行约定/前置），基线 ~15.1k。
          python3 scripts/context_budget.py --budget-tokens 15500

      - name: Build OpenClaw skill
        run: python3 scripts/build_openclaw.py

      - name: Commit dist/openclaw if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add SKILL.md dist/openclaw/
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit --amend --no-edit
            git push --force-with-lease
          fi
